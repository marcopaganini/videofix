# Changelog

## v0.1.2
- Process all `.mkv` files in a directory instead of just the largest one.
  This is useful for collections of media files that would otherwise not
  be properly processed.

## v0.1.1
- Added missing `install.sh` file.

## v0.1.0
- Initial release
